# macOS
.DS_Store
._*

# editors
.vscode/
.idea/
*.swp

# local backups exported from the app
liftlog-backup-*.json

# build scratch (the generators read from /tmp)
*.tar.gz
tools/.cache/
server/.vapid-secrets

# Supabase CLI local state: which project this checkout is linked to
supabase/.temp/

# dev tooling
node_modules/
test-results/
playwright-report/
